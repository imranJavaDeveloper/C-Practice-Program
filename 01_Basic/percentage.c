#include<stdio.h>
int mian(){

    /* find the percentage of five marks*/

    int math=78;
    int chem=63;
    int phy=65;
    int bio=58;
    int eng=85;

    // find percentage 
    int  per=(math + chem + phy + bio + english) / 5;

    // print the percentage value
    printf("%d",per);
    
    return 0;

}